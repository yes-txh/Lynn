package common.file;

message CompressionHeader {
    // See compression_type in compress/block_compression_codec.h
    optional uint32 compression_codec = 1;
    optional uint32 uncompressed_size = 2;
}

message RecordIOExtentHeader {
    optional CompressionHeader compression_header = 1;
}
