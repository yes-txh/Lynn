1 install cmake-2.8.10.2, glog-0.3.2, thrift-0.9.0, gtest-1.3.0
2 install libvirt kvm lxc
3 set Environment Varialbes:
    $ vim ~/.bashrc
    Enter the following content:
        LYNN_HOME=***
        export LYNN_HOME
    source ~/.bashrc
4 build:
    cd $LYNN_HOME
    mkdir build
    cd build
    cmake ..      # twice
    make
    make install  # install scheduler collector executor into bin/
